apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hellomct.fullname" . }}-test-connection"
  labels:
    {{- include "hellomct.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hellomct.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
